<h1>New link to entity <%=h @entity_id %> </h1>

<%= error_messages_for :link %>

<% form_for(:link, :url => links_path) do |f| %>

<%= render(:partial => 'form', :object => f) %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', links_path %>